#include<bits/stdc++.h>
using namespace std;
void finddiv(int d[],int n)
{
	for(int i=1;i*i<n;i++)
	{
		if(!(n%i))
	}
}
int main()
{
	int t;
	cin>>t;
	while(t!=0)
	{
		int n;
		cin>>n;
		long long int a[n];
		cin>>a[0];
		long long int divisor[1000001];
		finddiv(divisor,a[0]);
		
		for(int i=1;i<n;i++)
		{
			cin>>a[i];
		int max=0;
		int curr=0;
	
		cout<<max<<"\n";
		t--;
	}
}
